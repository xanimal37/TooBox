import { ProjectMaterialId } from './project-material-id';

describe('ProjectMaterialId', () => {
  it('should create an instance', () => {
    expect(new ProjectMaterialId()).toBeTruthy();
  });
});
